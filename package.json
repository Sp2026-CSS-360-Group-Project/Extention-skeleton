{
  "name": "focuskit-extension",
  "version": "1.0.0",
  "description": "FocusKit Manifest V3 Chrome productivity extension",
  "scripts": {
    "test": "jest --runInBand",
    "smoke": "playwright test tests/smoke.extension.spec.js",
    "check": "npm test && npm run smoke",
    "lint": "node static-analysis/eslint.js",
    "format": "prettier --write \"**/*.js\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/chrome": "^0.1.40",
    "eslint": "^10.3.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^30.4.1"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/*.test.js"
    ]
  }
}
